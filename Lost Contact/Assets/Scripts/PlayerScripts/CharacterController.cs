using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterController : MonoBehaviour
{
	List<GameObject> playerCharacters; //change the script

	GameObject currentHighlightedUnit = null;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

	public void selectUnit(TileNode tile)
	{

	}

	public void moveUnit(TileNode tile)
	{

	}
}
